<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/actividad_prinicipal"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/login"
    tools:context="com.natura.formosa.Vista.ActividadPrincipal">
    
    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:background="@drawable/natura_logo"/>

    <TextView
        android:id="@+id/consultora"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:layout_centerInParent="true"
        android:layout_below="@+id/logo"
        android:text="Consultora"
        android:textSize="10dp"
        android:textColor="#ffffff"/>

    <View
        android:id="@+id/view"
        android:layout_width="110dp"
        android:layout_height="1dp"
        android:layout_below="@+id/consultora"
        android:layout_centerInParent="true"
        android:background="#fc7dae"/>

    <TextView
        android:id="@+id/nombre"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_below="@+id/view"
        android:text="Wilma Riquelme"
        android:textColor="#ffffff"
        android:textSize="13dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="bottom"
        android:orientation="vertical">

        <com.shobhitpuri.custombuttons.GoogleSignInButton
            android:id="@+id/login_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="5dp"
            android:layout_centerInParent="true"
            android:text="Inicia sesión con Google"
            app:isDarkTheme="true" />

    </LinearLayout>

</RelativeLayout>
